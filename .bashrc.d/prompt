source $HOME/.git-prompt.sh

GIT_PS1_SHOWCOLORHINTS=yes

if [ "$HOSTNAME" = "mmazur-thinkpadt14gen4.rmtpl.csb" ]; then
	MYHOSTNAME=t14g4
else
	MYHOSTNAME=$HOSTNAME
fi

[ -z "$USER" ] && export USER=$USERNAME

if [ "$USER" == "vault" ]; then
	# Make it RED
	export PS1='[\[\e[31m\]\u@$MYHOSTNAME\[\e[0m\] $(basename $(dirname $(pwd)))/\W$(__git_ps1 " (%s)")]\$ '
else
	export PS1='[\u@$MYHOSTNAME $(basename $(dirname $(pwd)))/\W$(__git_ps1 " (%s)")]\$ '
fi

